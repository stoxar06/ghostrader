"""Knowledge layer — exports the engine's honest findings as an Obsidian vault.

A research memory, not a trading brain: every config, symbol, harness and concept
becomes a linked markdown 'neuron' so accumulated findings survive across sessions
and never have to be re-derived. See `vault.py`.
"""
